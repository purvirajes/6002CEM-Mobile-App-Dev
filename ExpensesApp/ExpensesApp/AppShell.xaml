<?xml version="1.0" encoding="UTF-8" ?>
<Shell xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
       xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
       xmlns:views="clr-namespace:ExpensesApp.Views"
       x:Class="ExpensesApp.AppShell">

    <ShellContent
        Title="Login"
        ContentTemplate="{DataTemplate views:LoginPage}"
        Route="LoginPage" />

    <ShellContent
        Title="Register"
        ContentTemplate="{DataTemplate views:RegisterPage}"
        Route="RegisterPage" />

    <ShellContent
        Title="Main"
        ContentTemplate="{DataTemplate views:MainAppPage}"
        Route="MainAppPage" />

    <ShellContent
    Title="Add Expense"
    ContentTemplate="{DataTemplate views:AddExpensePage}"
    Route="AddExpensePage" />

    <ShellContent 
        Title="Edit Expense"
        ContentTemplate="{DataTemplate views:EditExpensePage}"
        Route="EditExpensePage" />
    
    
    

</Shell>